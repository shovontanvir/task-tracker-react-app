import useMutateData from "../hooks/useMutateData";
import { postData } from "./ApiAction";

const PostHandler = (url, sentTask) => {
  //   const { mutate } = useMutateData(url, sentTask);
};

export default PostHandler;
